// const superHeros: string  [] = []
const superHeros: Array<string> = []

type user = {
    name: string,
    id: number
};

const users: user[] = [{name: 'muse', id: 1}]



const details: string[][] = []  // this means i want my array accepts array the arays is only string

export {}





superHeros.push('muse')
users.push({name: 'muse', id:1})
details.push(['Muse'],['hello'])